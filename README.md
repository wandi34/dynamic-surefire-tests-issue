# Example project for Dynamic Tests with Surefire

## How to reproduce
Just run `mvn test`: The names of the runned tests are as described as in the issue

In an IDE (like IntelliJ) the names will be shown correctly
